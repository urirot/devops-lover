# Containerize a gatsby-react app with Docker

## Getting started

Build the container
`docker build -t devops-console .`

Then you can run the container, passing in a name for the container
`docker run -p 8000:80 devops-console`
